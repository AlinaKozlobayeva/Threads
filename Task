Вручную создать каталог, в котором разместить 9 файлов txt. Каждый файл будет
именоваться 1.txt, 2.txt, 3.txt и т.д. В каждом файле будет заполняющий символ, который
соответствует названию файла (в файле 1.txt будет “1”). В каждом файле должно быть по 100
строк, каждая строка длиной в 100 символов.

Написать программу, которая читает содержимое этих файлов в результирующий файл
super.txt, при помощи нескольких потоков. Количество потоков должно указываться в property
файле.